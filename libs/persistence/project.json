{
  "name": "persistence",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/persistence/src",
  "projectType": "library",
  "targets": {
    "prisma-generate": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "npx prisma generate --schema libs/persistence/prisma/schema.prisma"
        ],
        "parallel": false
      }
    },
    "db-migrate": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "npx prisma migrate deploy --schema libs/persistence/prisma/schema.prisma"
        ]
      }
    },
    "db-studio": {
        "executor": "nx:run-commands",
        "options": {
          "commands": [
            "npx prisma studio"
          ],
          "envFile": ".env.development"
        }
      }
  },
  "tags": []
}
